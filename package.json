{
  "name": "aether-bloom",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "deploy:cloudflare": "npm run build && npx wrangler pages deploy dist"
  },
  "dependencies": {
    "esbuild": "^0.27.4",
    "three": "^0.183.2"
  }
}
